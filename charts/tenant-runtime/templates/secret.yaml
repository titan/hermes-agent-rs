{{- if .Values.runtimeSecret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tenant-runtime.runtimeSecretName" . }}
  labels:
    {{- include "tenant-runtime.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- toYaml .Values.runtimeSecret.stringData | nindent 2 }}
{{- end }}
